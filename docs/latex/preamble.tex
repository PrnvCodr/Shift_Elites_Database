% ============================================================
%  Shift_Elite DB --- LaTeX Preamble
%  Packages, colours, environments, macros
% ============================================================
\documentclass[12pt,a4paper,twoside,openright]{report}

% ---------- encoding / fonts ----------
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{microtype}

% ---------- geometry ----------
\usepackage[margin=2.5cm,headheight=15pt]{geometry}

% ---------- language ----------
\usepackage[english]{babel}

% ---------- colours ----------
\usepackage[dvipsnames,svgnames,x11names]{xcolor}
\definecolor{shiftPrimary}{HTML}{6C63FF}
\definecolor{shiftAccent}{HTML}{00C9A7}
\definecolor{shiftDark}{HTML}{1A1A2E}
\definecolor{shiftWarn}{HTML}{FF6B6B}
\definecolor{shiftBg}{HTML}{F5F5FA}
\definecolor{codeGray}{HTML}{2D2D2D}
\definecolor{codeBg}{HTML}{F4F4F8}

% ---------- headers / footers ----------
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[LE]{\small\textit{\leftmark}}
\fancyhead[RO]{\small\textit{\rightmark}}
\fancyfoot[C]{\thepage}
\renewcommand{\headrulewidth}{0.4pt}
\renewcommand{\footrulewidth}{0pt}

% ---------- section titles ----------
\usepackage{titlesec}
\titleformat{\chapter}[display]
  {\normalfont\huge\bfseries\color{shiftPrimary}}
  {\chaptertitlename\ \thechapter}{20pt}{\Huge}
\titleformat{\section}
  {\normalfont\Large\bfseries\color{shiftDark}}
  {\thesection}{1em}{}
\titleformat{\subsection}
  {\normalfont\large\bfseries\color{shiftDark!80}}
  {\thesubsection}{1em}{}

% ---------- maths ----------
\usepackage{amsmath,amssymb,amsthm}

% ---------- graphics / tikz ----------
\usepackage{graphicx}
\usepackage{tikz}
\usetikzlibrary{
  shapes.geometric, arrows.meta, positioning,
  calc, fit, backgrounds, decorations.markings,
  trees, matrix, chains
}

% TikZ styles used across chapters
\tikzset{
  component/.style = {
    rectangle, rounded corners=4pt, draw=shiftPrimary,
    fill=shiftPrimary!8, text=shiftDark, minimum width=3.2cm,
    minimum height=1cm, font=\small\bfseries, align=center,
    line width=0.7pt
  },
  storage/.style = {
    cylinder, draw=shiftAccent, fill=shiftAccent!10,
    shape border rotate=90, aspect=0.25,
    minimum height=1.2cm, minimum width=2cm,
    font=\small\bfseries, align=center
  },
  arr/.style = {
    -Stealth, thick, color=shiftDark!70
  },
  dasharr/.style = {
    -Stealth, dashed, color=shiftDark!50
  },
  note/.style = {
    rectangle, rounded corners=2pt, draw=shiftWarn!70,
    fill=shiftWarn!5, font=\footnotesize\itshape,
    text width=4cm, align=left
  },
  page/.style = {
    rectangle, draw=shiftDark, fill=shiftBg,
    minimum width=3cm, minimum height=2cm,
    font=\footnotesize, align=center
  },
  slot/.style = {
    rectangle, draw=shiftPrimary!60, fill=shiftPrimary!10,
    minimum width=2.6cm, minimum height=0.4cm,
    font=\scriptsize, align=center
  },
  btreenode/.style = {
    rectangle split, rectangle split horizontal,
    rectangle split parts=#1, draw=shiftPrimary,
    fill=shiftPrimary!8, font=\footnotesize,
    minimum height=0.6cm
  },
}

% ---------- listings (code) ----------
\usepackage{listings}
\lstset{
  basicstyle=\ttfamily\small,
  backgroundcolor=\color{codeBg},
  frame=single,
  rulecolor=\color{shiftPrimary!30},
  framesep=6pt,
  xleftmargin=8pt,
  xrightmargin=8pt,
  breaklines=true,
  breakatwhitespace=false,
  tabsize=4,
  showstringspaces=false,
  numbers=left,
  numberstyle=\tiny\color{gray},
  numbersep=8pt,
  keywordstyle=\bfseries\color{shiftPrimary},
  stringstyle=\color{OliveGreen},
  commentstyle=\itshape\color{gray},
  captionpos=b,
  aboveskip=10pt,
  belowskip=8pt,
}

\lstdefinelanguage{SQL}{
  morekeywords={SELECT, FROM, WHERE, INSERT, INTO, VALUES,
    UPDATE, SET, DELETE, CREATE, TABLE, DROP, ALTER, ADD,
    INDEX, ON, UNIQUE, AND, OR, NOT, IN, LIKE, BETWEEN,
    IS, NULL, ASC, DESC, AS, INNER, LEFT, RIGHT, FULL,
    OUTER, CROSS, JOIN, GROUP, BY, HAVING, ORDER, LIMIT,
    OFFSET, COUNT, SUM, AVG, MIN, MAX, PRIMARY, KEY,
    FOREIGN, REFERENCES, DISTINCT, EXPLAIN, SHOW, TABLES,
    DESCRIBE, BEGIN, COMMIT, ROLLBACK, INT, VARCHAR, FLOAT,
    BOOLEAN, DATE, TIMESTAMP, AUTO_INCREMENT, DEFAULT,
    NOT_NULL, IF, EXISTS, CASCADE},
  sensitive=false,
  morestring=[b]',
  morecomment=[l]{--},
}

\lstdefinestyle{cpp}{language=C++,
  morekeywords={uint32_t,int32_t,uint64_t,size_t,
    shared_ptr,unique_ptr,make_shared,make_unique,
    std,string,vector,map,unordered_map,variant,
    monostate,optional,mutex,lock_guard,atomic,
    nullptr,auto,constexpr,override,final,noexcept},
}

\lstdefinestyle{pseudo}{
  basicstyle=\ttfamily\small,
  mathescape=true,
  keywordstyle=\bfseries\color{shiftPrimary},
  morekeywords={function,return,if,else,for,while,
    do,end,then,and,or,not,true,false,null,
    procedure,begin,Input,Output},
  literate={<-}{{$\leftarrow$}}2 {->}{{$\rightarrow$}}2
            {>=}{{$\geq$}}1 {<=}{{$\leq$}}1
            {!=}{{$\neq$}}1,
}

% ---------- algorithm environment ----------
\usepackage[ruled,vlined,linesnumbered]{algorithm2e}
\SetAlgoLined
\SetKwInput{KwInput}{Input}
\SetKwInput{KwOutput}{Output}

% ---------- tables ----------
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{multirow}
\usepackage{longtable}

% ---------- links ----------
\usepackage[
  colorlinks=true, linkcolor=shiftPrimary,
  citecolor=shiftAccent, urlcolor=shiftPrimary!80
]{hyperref}
\usepackage[nameinlink]{cleveref}

% ---------- bibliography ----------
\usepackage[numbers,sort&compress]{natbib}

% ---------- misc ----------
\usepackage{etoolbox}
\usepackage{enumitem}
\usepackage{float}
\usepackage{caption}
\usepackage{subcaption}
\usepackage{tcolorbox}
\tcbuselibrary{skins,breakable}

% ---------- custom boxes ----------
\newtcolorbox{conceptbox}[1][]{
  enhanced, breakable,
  colback=shiftPrimary!3, colframe=shiftPrimary!60,
  fonttitle=\bfseries\color{white},
  coltitle=white, colbacktitle=shiftPrimary,
  title={#1}, arc=3pt, boxrule=0.5pt,
  left=6pt, right=6pt, top=4pt, bottom=4pt,
}

\newtcolorbox{interviewbox}[1][]{
  enhanced, breakable,
  colback=shiftAccent!3, colframe=shiftAccent!60,
  fonttitle=\bfseries\color{white},
  coltitle=white, colbacktitle=shiftAccent!80!black,
  title={#1}, arc=3pt, boxrule=0.5pt,
  left=6pt, right=6pt, top=4pt, bottom=4pt,
}

\newtcolorbox{warningbox}[1][]{
  enhanced, breakable,
  colback=shiftWarn!3, colframe=shiftWarn!60,
  fonttitle=\bfseries\color{white},
  coltitle=white, colbacktitle=shiftWarn!80!black,
  title={#1}, arc=3pt, boxrule=0.5pt,
  left=6pt, right=6pt, top=4pt, bottom=4pt,
}

\newtcolorbox{deepdivebox}[1][]{
  enhanced, breakable,
  colback=shiftDark!3, colframe=shiftDark!30,
  fonttitle=\bfseries\color{white},
  coltitle=white, colbacktitle=shiftDark!80,
  title={#1}, arc=3pt, boxrule=0.5pt,
  left=6pt, right=6pt, top=4pt, bottom=4pt,
}

% ---------- glossary-like commands ----------
\DeclareRobustCommand{\shiftdb}{\textsc{Shift\_Elite DB}}
\DeclareRobustCommand{\cmark}{\textcolor{shiftAccent}{\checkmark}}
\DeclareRobustCommand{\xmark}{\textcolor{shiftWarn}{\texttimes}}
\DeclareRobustCommand{\bigO}[1]{\ensuremath{\mathcal{O}(#1)}}
\DeclareRobustCommand{\code}[1]{\texttt{#1}}

% ---------- document metadata ----------
\title{%
  {\Huge\bfseries\color{shiftPrimary} Shift\_Elite DB}\\[6pt]
  {\Large Building a Database Management System from Scratch}\\[4pt]
  {\large A Deep-Learning \& Interview-Preparation Guide}
}
\author{Pranav Kashyap}
\date{\today}
